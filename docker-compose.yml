services:
  discord-to-slack-bot:
    build: .
    container_name: discord-to-slack-bot
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    # 설정 파일 경로 (기본값이 /app/config.yaml 이므로 생략 가능)
    # environment:
    #   - CONFIG_PATH=/app/config.yaml
